<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dd.qazaqsha.ertegiler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_aldar_kose"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_aldar_kose"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity android:name=".ActivityListCategories">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityListFairyTales"
            android:parentActivityName=".ActivityListCategories" />
        <activity
            android:name=".ActivityFairyTaleText"
            android:parentActivityName=".ActivityListFairyTales"/>
        <activity android:name=".DatabaseAccess" />
    </application>

</manifest>